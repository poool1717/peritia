# knowledge/causes/

Catálogo de causas de siniestro (`Cause`, ver
`docs/domain/entities/CAUSE.md`), con su categoría y su correspondencia con
las garantías que puede activar (ver `knowledge/ontology/ONTOLOGY.md`,
relación *Daño → puede estar causado por → Causa*).

**Relación con `knowledge/causas/`** (Sprint 0): mismo propósito, sin el
formato de este sprint. No se ha movido ni fusionado — ver reconciliación en
`KNOWLEDGE_ARCHITECTURE.md`. Sustituiría, en su forma madura, al mapa fijo
`CAUSA_COB` y a las expresiones regulares de `causasMeteo()` incrustados hoy
en `components/Peritia.jsx`.

Vacía.
