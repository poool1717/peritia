# knowledge/synonyms/

Diccionarios de sinónimos y variantes de escritura, en el formato de unidad
de conocimiento definido en `knowledge/normalization/NORMALIZATION.md`.

**Relación con `knowledge/sinonimos/`** (Sprint 0): mismo propósito, sin el
formato de este sprint. No se ha movido ni fusionado — ver reconciliación en
`KNOWLEDGE_ARCHITECTURE.md`. Sustituiría, en su forma madura, a
`normCompania()` y a la lógica de coincidencia de `matchBaremo()`
incrustadas hoy en `components/Peritia.jsx`.

Vacía.
