# knowledge/causas/

Catálogo de causas de siniestro y su relación con las garantías afectadas
(qué causa activa qué cobertura).

Vacía. Hoy esa relación está en el mapa `CAUSA_COB` y en las expresiones
regulares de `causasMeteo()` dentro de `components/Peritia.jsx`.
